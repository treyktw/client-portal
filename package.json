{
  "name": "telera-portal",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "web": "pnpm --filter web dev",
    "mobile": "pnpm --filter mobile start",
    "convex": "pnpm --filter @telera/convex dev",
    "lint": "turbo lint",
    "clean": "turbo clean && rm -rf node_modules",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.3.0"
  },
  "packageManager": "pnpm@8.15.0",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}